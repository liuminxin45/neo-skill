# skills/skill-creator/references

本目录存放 **skill-creator** 相关的参考资料与规则文档。

## 用途

- 存放较长、结构化的说明，以便让生成的主入口文档保持精炼。
- 被 `skillspec.json` 的 `references` 字段引用。

## 约定

- 这里的文档应尽量稳定可复用（面向“规则/模板/协议”），避免写一次性产出。
